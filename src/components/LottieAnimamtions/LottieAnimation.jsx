import React from "react";

export default function LottieAnimation({ name, animeUrl, icon }) {
  return (
    <div>
        {/* <lottie-player src={`${animeUrl}`}  background="transparent"  speed="1"  style={{width: '100%', height: 'auto'} }  loop  autoplay></lottie-player> */}

    </div>
  );
}
